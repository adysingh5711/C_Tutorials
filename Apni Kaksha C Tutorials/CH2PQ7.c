#include<stdio.h>
int main(){
   /* 
   int a =8^8 --> Valid
   int x; int y =x; --> Valid
   int x,y = x; --> Invalid (using the operation in the same line in which function is being declared)
   char stars = "**" --> Invalid (Since more than one value in char)
    */
   printf ("Problem Solved");
   return 0;
}